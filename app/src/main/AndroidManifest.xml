<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobile2.projeto2">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />
    <application
        android:name="com.mobile2.projeto2.Project2Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.syllable_activity.SyllableActivityActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden">
        </activity>
        <activity android:name=".activities.criar_template.CriarTemplateActivity">

        </activity>
        <activity android:name=".activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.wordselector_activity.WordSelectorActivity" />
        <activity android:name=".activities.management.ManagementActivity" />
        <activity android:name=".activities.choosetemplate.ChooseTemplateActivity" />
        <activity android:name=".activities.criar_template_video.CriarTemplateActivityVideo" />
        <activity android:name=".activities.feedback.FeedbackActivity" />
        <activity android:name=".activities.video_activity.VideoActivity" />
        <activity android:name=".activities.wordselector_activity.deletion.WordDeletionActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.mobile2.Projeto2.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
    </application>

</manifest>